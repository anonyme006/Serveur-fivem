# [ox]

Ressources Overextended : oxmysql, ox_lib, ox_inventory, ox_target, ox_doorlock, ox_fuel.

```bash
./scripts/install-dependencies.sh
```

Compilez `ox_lib` / `ox_inventory` avec pnpm si les builds web manquent.
