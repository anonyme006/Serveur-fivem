Config = {}

-- Affiche l'overlay dès que le joueur est down / mort (qbx_medical)
Config.ShowOnDeath = true

-- Texte principal (tagline RE ROLL)
Config.Tagline = "Mourir n'est plus un choix"

-- Secondes avant de proposer l'appel EMS / respawn (UI only)
Config.BleedOutSeconds = 300

-- Touche pour fermer l'overlay une fois réanimé (auto close aussi)
Config.AutoCloseOnRevive = true
